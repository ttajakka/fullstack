import infoReducer from './infoReducer'
import deepFreeze from 'deep-freeze'

describe('infoReducer', () => {
  test('returns new sttate with action NEW_INFO', () => {
    let state = ''

    deepFreeze(state)
  })
})